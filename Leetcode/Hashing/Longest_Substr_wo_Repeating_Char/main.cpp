#include <iostream>
#include <string>
#include <unordered_set>
using namespace std;

int lengthOfLongestSubstring(string s)
{
  unordered_set<char> mapping; 
   for(auto c: s)
    {
      if (!mapping.find(c)){
        mapping.
    }
  } 

}


int main()
{
  cout << "Hello";
  return 0;
}
