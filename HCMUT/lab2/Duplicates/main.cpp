#include <iostream>
#include <string>
#include <stack>
#include <vector>

std::string removeDuplicates(std::string S){
    std::stack<char> st;
    for(auto c in S){
        char tmp = st.pop();

    }

}
